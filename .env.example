# Example environment variables for local development
# Copy this file to .env (or set these env vars in your system) and replace the values.
# Do NOT commit real passwords to VCS.

# Database
DB_URL=jdbc:mysql://localhost:3306/vms_db?createDatabaseIfNotExist=true
DB_USERNAME=root
DB_PASSWORD=changeme

# Mail
MAIL_USERNAME=youremail@example.com
MAIL_PASSWORD=changeme
